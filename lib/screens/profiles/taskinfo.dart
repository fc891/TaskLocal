//TaskInfo object represents all info that needs to be kept/displayed regarding completed tasks
class TaskInfo {
  final String taskInfo;
  final int taskNumber;
  const TaskInfo(this.taskInfo, this.taskNumber);
}
